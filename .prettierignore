node_modules
build
.svelte-kit
bun.lock
package-lock.json

# Generated, and whitespace-significant: the portrait is a <pre> of half-block
# characters where a reflow would visibly break the picture.
src/lib/components/PixelPortrait.svelte

# Copied in by `bun run fonts` / `bun run og`.
static/fonts
static/og

# written by the resume builder, committed through GitHub as-is
src/lib/data/profile.json
src/lib/data/resume.json
